<?phpif (!defined('BASEPATH'))      exit('No direct script access allowed');require COMMONPATH . 'third_party/devices/Mobile_Detect.php';if (!function_exists('get_device')) {      function get_device() {            $detect = new Mobile_Detect;            return $deviceType = ($detect->isMobile() ? ($detect->isTablet() ? 'tablet' : 'phone') : 'computer');      }}if (!function_exists('is_Iphone')) {      function is_Iphone() {            $detect = new Mobile_Detect;            $retorno= $detect->isIphone();            return $retorno;      }}if (!function_exists('get_aparelho')) {      function get_aparelho() {            $detect = new Mobile_Detect;            $rules=$detect->getRules();                                                foreach($rules as $name => $regex){              $check = $detect->{'is'.$name}();              if ($check){                $retorno[]= $name;                  }                      }        return $retorno;                  }}                                                                                                    